/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: false,
    images: {
        domains: ['img.freepik.com','www.freepik.com', 'lh3.googleusercontent.com', 'images.unsplash.com','www.pexels.com'],
    },
};

export default nextConfig;
